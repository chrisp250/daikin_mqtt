[General]

[mqtt]
server = #IP address of MQTT broker
port = 1883
username = # Remove if MQTT broker doesn't require authentication
password = # Remove if MQTT broker doesn't require authentication
base_topic = climate/studio

[unit]
ip = #AC unit IP address
